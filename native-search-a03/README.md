# Exact A03 native search

`HighDensitySolverA03` accepts `useNativeSearch: true` to use a separate, synchronously initialized WebAssembly search backend when its inputs and hooks are supported. The option defaults to false. TypeScript owns setup, budgets, public iteration counts, finalization, ownership mutation, retry scheduling and visualization. Native batches consume only ordinary search pops and leave goal, empty-heap and budget boundaries to the original step. A01 source and generated bytes are unchanged.

The backend is selected on the first search. Unsupported initial inputs use JavaScript; unsupported live changes first materialize the current native state and continue the original JavaScript suffix. After declining, that solver stays in JavaScript until setup is reset. Supported graph data has bounded, nonempty dimensions, canonical CSR indices, finite cell centers and non-NaN Float32 edge costs. Positive and negative infinity edges have exact state controls. Numeric cost changes remain live. Customized callbacks, accessors and unsupported storage use the guarded JavaScript path.

The module imports the same engine's `Math.hypot` and the original lazy TypeScript footprint function. It preserves heap ties, persistent rip state, Float32 edge widening and arithmetic order. Its distance memo stores only the exact imported result. FIFO goal tables have at most 65,536 total slots: three Float64 vectors and one validity-byte vector per table, at most 1,638,400 numeric bytes plus map/vector metadata. Both coordinate differences are recomputed before lookup. Signed zeros remain distinct; the memo uses same-value NaN comparison. Via and greedy costs remain live.

The original TypeScript start heuristic runs before each native search. Its completed cache slot seeds the native memo before the first pop. The additive cache snapshot exports every table in insertion order, including unused raw float slots and validity bytes. Materialization restores the TypeScript Map, surviving table identities and FIFO accounting before customized callbacks can observe them. Search and distance caches survive subsequent native searches and clear at setup/release.

`ABI-DRAFT.md` describes the implemented ABI. Search-state kind40 and distance-cache kind41 remain complete serialized correctness oracles. Production kinds42/43 expose bulk array spans and FIFO distance-buffer metadata, avoiding serialized copies and temporary distance arrays. Full materialization still retains heap/node/rip backing slots, both visited arrays, best-G values/stamps, move scratch outputs, ordered occupants and layer stamps. The original TypeScript footprint Map keeps its independent lifetime, so bulk transfer omits duplicate native footprint contents. Decoders preserve raw floating backing bytes, NaN payloads, signed zero, existing cache-array identities and FIFO order. All borrowed views are copied synchronously before another mutating export or memory growth.

The bridge prevents reentry. A host footprint callback copies only into its supplied preallocated destination and does not call exports or grow native memory. Imported exceptions propagate without replay. No Rust borrow guard or temporary allocation needs destruction across an import. After unwind, `a03_publish_state` exposes the partial heap and attempted/completed counters.

## Build and correctness checks

The frozen reference captures use Bun 1.3.2. The generated module uses Rust 1.93.1 with the `wasm32-unknown-unknown` target. Production Rust has no external dependency; `flate2` is a development dependency for compressed reference fixtures. The normal checks use committed fixtures and do not require a separate reference checkout.

```sh
cargo +1.93.1 test --locked --manifest-path native-search-a03/Cargo.toml
bun scripts/build-native-a03-search.ts
git diff --exit-code -- lib/native-search/a03SearchWasmBytes.ts
bun native-search-a03/tests/wasm-oracle.ts
bun native-search-a03/tests/wasm-lifecycle.ts
bun native-search-a03/tests/distance-seed-oracle.ts
bun native-search-a03/tests/distance-view-oracle.ts
RUSTFLAGS="-C link-arg=-zstack-size=65536" cargo +1.93.1 build --locked --manifest-path native-search-a03/Cargo.toml --target wasm32-unknown-unknown --release --features uncached-distance-oracle --target-dir native-search-a03/target-oracle
bun native-search-a03/tests/wasm-oracle.ts --uncached native-search-a03/target-oracle/wasm32-unknown-unknown/release/a03_exact_search_preparation.wasm
```

The test-only `uncached-distance-oracle` feature preserves the original complete host-call sequence. Production builds enable the memo. Both module search oracles also compare every bulk-view backing value against the unchanged full snapshot, including partial host failures. The direct distance-view oracle compares all 55 frozen C49 seed/FIFO boundaries against kind41. Graph-only validation controls retain every graph check and require full ownership validation at begin. Generated test summary JSON is ignored; the reference manifests and compressed inputs are committed.

## Reference provenance

To regenerate reference fixtures, provide the exact public C37 or C49 checkout. Each capture checks the source file against its immutable Git blob before execution and records source, generator, raw and compressed content hashes.

```sh
# C37: 8e363ef2a5a0449f03b8004a8d150b6b1b343092
BUN_UPDATE_SNAPSHOTS=1 bun native-search-a03/tests/capture-reference.ts /path/to/exact-c37-checkout
# C49: e2e837d1379f4e2cb7cef9abddaf5408919a0012
bun native-search-a03/tests/distance-seed-oracle.ts --capture /path/to/exact-c49-checkout
```

The C37 oracle contains 28 search captures with 2,041 boundaries: six fixtures with first/later searches, synthetic 2/4/6-layer ownership, five host failures, live numeric costs and positive/negative infinity edges. Goal, empty, failure and capped-prefix outcomes are labeled explicitly. Another 8,200 operations compare complete heap backing arrays through growth, ties, signed zeros, infinities and NaNs. The actual uncached module replays 23 first-search captures; the cached module replays 21 while proving positive hits and partial exception state. Rust snapshot restoration covers later-search backing state. The C49 oracle checks 55 start-seed boundaries, every raw cache slot, FIFO evictions, repeated/invalid seeds and the oversized-grid capacity of zero.

These are correctness checks, not timing benchmarks or a performance claim.
